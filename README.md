# BAR estimator of FEP output

This is a python script to calculate the free energies from NAMD FEP output by BAR estimator.

Usage:

    ./BAR.py --forward complex-forward.fepout --backward complex-backward.fepout